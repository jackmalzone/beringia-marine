# Brand assets (staging)

**Intended contents:** Logo PNG used in header (`logo-white-transparent.svg` alternatives in `components-migration/assets/beringia/`).

**Status:** No binary files copied per migration policy. See `metadata/asset-inventory.md`.

**Phase 2:** Drop canonical brand files here after recovery from production or backup, then point template config at them.
